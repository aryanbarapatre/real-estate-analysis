from django.contrib import admin

# Register your models here.
# Currently no models are registered as we're working directly with Excel/CSV files
# If you create models in models.py, register them here like:
# from .models import RealEstateData
# admin.site.register(RealEstateData)

